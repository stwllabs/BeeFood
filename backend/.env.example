# Local development (SQLite) — change provider in prisma/schema.prisma to "sqlite"
# DATABASE_URL="file:./dev.db"

# Production (PostgreSQL) — used by Render and other cloud hosts
DATABASE_URL="postgresql://user:password@localhost:5432/beefood"

JWT_SECRET=change-me-in-production
PORT=5000
